import React from 'react';
import styled from 'styled-components';

const Home: React.FC = () => {
  return <StyledPage>This is home</StyledPage>;
};

const StyledPage = styled.div``;
export default Home;
